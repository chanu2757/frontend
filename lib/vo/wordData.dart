class Data {
  Data({
    required this.date,
    required this.word,
    required this.meaning,
    required this.isSelected,
  });
  
  DateTime date;
  String word;
  String meaning;
  bool isSelected;
}